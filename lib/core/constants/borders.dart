import 'package:flutter/material.dart';

abstract class Borders {
  static final BorderRadius answerButtonBorderRadius = BorderRadius.circular(20);
  static final BorderRadius all100 = BorderRadius.circular(100);
  static final BorderRadius all12 = BorderRadius.circular(12);
}
